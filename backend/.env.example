# Local development only. Bootstrap never overwrites an existing .env.
DEBUG=True
SECRET_KEY=development-only-replace-before-deployment
ALLOWED_HOSTS=localhost,127.0.0.1,10.0.2.2
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
SECURE_SSL_REDIRECT=False
GEMINI_API_KEY=
OPENROUTER_API_KEY=
# Optional PostgreSQL configuration (SQLite is the local default).
# POSTGRES_DB=tersoopilot
# POSTGRES_USER=tersoopilot
# POSTGRES_PASSWORD=
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# Explicit opt-in for AI tool writes; staff access is still required.
ASSISTANT_ALLOW_WRITES=False

# Explicit opt-in for AI tool writes; staff access is still required.
ASSISTANT_ALLOW_WRITES=False
